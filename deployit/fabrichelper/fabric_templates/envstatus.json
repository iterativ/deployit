{
    "lastDeployment": "{{ envStatusLastDeployment }}", 
    "gitBranch": "{{ envStatusGitBranch }}",
    "gitChecksum": "{{ envStatusGitChecksum }}",
    "gitTimestamp": "{{ envStatusLastCommitDate }}"
}  
